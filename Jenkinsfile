pipeline {
  agent { label 'master' }
  stages {
    stage('test') {
      steps {
        echo 'test'
      }
    }
    stage('build') {
      steps {
        echo 'build'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }
  }
}